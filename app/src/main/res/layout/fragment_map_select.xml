<?xml version="1.0" encoding="utf-8"?>
<!--
    STEP 2 – MAP SELECTION
    Horizontal RecyclerView with map cards
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/color_background">

    <!-- Header -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/map_select_title"
        android:textSize="22sp"
        android:textColor="@color/color_primary"
        android:textStyle="bold"
        android:fontFamily="@font/orbitron"
        android:letterSpacing="0.1"
        android:layout_marginStart="@dimen/padding_screen"
        android:layout_marginTop="@dimen/spacing_md"
        android:layout_marginBottom="16dp" />

    <!-- Horizontal map list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMaps"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/padding_screen"
        android:paddingEnd="@dimen/padding_screen"
        android:clipToPadding="false"
        android:overScrollMode="never"
        tools:listitem="@layout/item_map_card" />

    <!-- Map preview / description panel -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardMapPreview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginStart="@dimen/padding_screen"
        android:layout_marginEnd="@dimen/padding_screen"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="8dp"
        app:cardBackgroundColor="@color/color_surface"
        app:cardCornerRadius="@dimen/radius_lg"
        app:cardElevation="4dp"
        app:strokeColor="@color/color_primary"
        app:strokeWidth="1dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="20dp">

            <!-- Map name -->
            <TextView
                android:id="@+id/tvSelectedMapName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_subtitle"
                android:textColor="@color/color_primary"
                android:textStyle="bold"
                android:fontFamily="@font/orbitron"
                android:letterSpacing="0.1"
                tools:text="Beginner Maze" />

            <!-- Stars row -->
            <LinearLayout
                android:id="@+id/starsContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp">

                <ImageView android:id="@+id/star1" android:layout_width="20dp" android:layout_height="20dp" android:src="@drawable/ic_star_filled" android:contentDescription="@null" />
                <ImageView android:id="@+id/star2" android:layout_width="20dp" android:layout_height="20dp" android:src="@drawable/ic_star_empty" android:contentDescription="@null" android:layout_marginStart="2dp" />
                <ImageView android:id="@+id/star3" android:layout_width="20dp" android:layout_height="20dp" android:src="@drawable/ic_star_empty" android:contentDescription="@null" android:layout_marginStart="2dp" />
                <ImageView android:id="@+id/star4" android:layout_width="20dp" android:layout_height="20dp" android:src="@drawable/ic_star_empty" android:contentDescription="@null" android:layout_marginStart="2dp" />
                <ImageView android:id="@+id/star5" android:layout_width="20dp" android:layout_height="20dp" android:src="@drawable/ic_star_empty" android:contentDescription="@null" android:layout_marginStart="2dp" />

            </LinearLayout>

            <!-- Difficulty label -->
            <TextView
                android:id="@+id/tvSelectedMapDiff"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_caption"
                android:textColor="@color/color_diff_easy"
                android:fontFamily="@font/rajdhani"
                android:textStyle="bold"
                android:letterSpacing="0.2"
                android:layout_marginTop="6dp"
                tools:text="EASY" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>
