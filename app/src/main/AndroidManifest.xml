<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Sensor features -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.proximity" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiltArena"
        tools:targetApi="31">

        <!-- Splash -->
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TiltArena">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".activity.LoginActivity" android:exported="false" />
        <activity android:name=".activity.RegisterActivity" android:exported="false" />

        <!-- Home -->
        <activity android:name=".activity.HomeActivity" android:exported="false" />
        <activity android:name=".activity.ProfileActivity" android:exported="false" />
        <activity android:name=".activity.SettingsActivity" android:exported="false" />
        <activity android:name=".activity.LeaderboardActivity" android:exported="false" />

        <!-- Room Management -->
        <activity android:name=".activity.CreateRoomActivity" android:exported="false" />
        <activity android:name=".activity.JoinRoomActivity" android:exported="false" />
        <activity android:name=".activity.LobbyActivity" android:exported="false" />
        <activity android:name=".activity.MatchmakingActivity" android:exported="false" />

        <!-- Game -->
        <activity
            android:name=".activity.GameActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            android:keepScreenOn="true" />

        <!-- Result -->
        <activity android:name=".activity.ResultActivity" android:exported="false" />

        <!-- Legacy activities (if needed) -->
        <activity android:name=".activity.MainActivity" android:exported="false" />
        <activity android:name=".activity.PlaySetupActivity" android:exported="false" />
        <activity android:name=".activity.ScoreActivity" android:exported="false" />

    </application>

</manifest>